compil Prog()
{
    /* Declaration */
    intCompil x, y;
    floatCompil n, s;

    start
    scanCompil(n);
    x = 1;
    y = x - 1 + 5;

    if ( x > 10 ) then {
        x = x + 1;
    }

    do {
        y = 1 + y * 2;
    } while (x < 0)

    printCompil("hello");
    printCompil(x);

    /* Ceci est un commentaire
    sur plusieurs lignes
    */

}
